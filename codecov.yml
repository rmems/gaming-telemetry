coverage:
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: 80%

# build.rs configures compilation and is not production runtime behavior.
ignore:
  - build.rs
